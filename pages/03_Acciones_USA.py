
import streamlit as st import pandas as pd import numpy as np import matplotlib.pyplot as plt import seaborn as sns  # 1. CONFIGURACIÓN INICIAL st.set_page_config(page_title="Performance Avanzada - USA", layout="wide")  @st.cache_data def load_data():     try:         # Cargamos detectando separador automáticamente         df = pd.read_csv('reporte_trades_para_USA.csv', sep=None, engine='python')                  # Limpieza agresiva de encabezados         df.columns = [str(c).strip().lower() for c in df.columns]                  # Mapeo de normalización         mapeo = {             'ticker': 'Ticker', 'inversion_usa': 'Inversion_USA',             'estado_trade': 'Estado_Trade', 'fecha': 'fecha',             'cantidad_usa': 'Cantidad_USA', 'precio_unitario': 'Precio_Unitario',             'id_trade': 'ID_Trade'         }         df = df.rename(columns={k: v for k, v in mapeo.items() if k in df.columns})                  # Procesamiento de fecha con fallback         if 'fecha' in df.columns:             df['fecha'] = pd.to_datetime(df['fecha'], errors='coerce')         else:             # Si no se llama 'fecha', buscamos columnas similares             cols_fec = [c for c in df.columns if 'fec' in c or 'dat' in c]             if cols_fec:                 df = df.rename(columns={cols_fec[0]: 'fecha'})                 df['fecha'] = pd.to_datetime(df['fecha'], errors='coerce')                  return df     except Exception as e:         st.error(f"❌ Error crítico al leer el CSV: {e}")         return None  # --- INICIO DE LA APP --- st.title("🚀 Dashboard de Trading de Alta Precisión (Benchmark 10k)") df_trades = load_data()  if df_trades is not None:     try:         # --- 2. PROCESAMIENTO (Named Aggregation para evitar KeyError) ---         df_cerrados = df_trades[(df_trades['Estado_Trade'] == 'Cerrado') & (df_trades['fecha'].notnull())].copy()          # Agrupamos creando columnas nuevas explícitas         resumen_stats = df_cerrados.groupby(['Ticker', 'ID_Trade']).agg(             Fecha_In=('fecha', 'min'),             Fecha_Out=('fecha', 'max'),             Neto_Flujo=('Inversion_USA', 'sum'),             Cant_Total=('Cantidad_USA', lambda x: x[x > 0].sum()),             Precio_Entrada=('Precio_Unitario', 'first')         ).reset_index()          # Cálculos de Performance         resumen_stats['Resultado_USD'] = resumen_stats['Neto_Flujo']         resumen_stats['Inversion_Inicial'] = resumen_stats['Cant_Total'] * resumen_stats['Precio_Entrada']         resumen_stats['Rendimiento_%'] = np.where(resumen_stats['Inversion_Inicial'] > 0,                                                  (resumen_stats['Resultado_USD'] / resumen_stats['Inversion_Inicial']) * 100, 0)                  # Métrica solicitada: Tamaño vs cuenta de 10k         resumen_stats['Size_vs_10k_%'] = (resumen_stats['Inversion_Inicial'] / 10000) * 100          ganadores = resumen_stats[resumen_stats['Resultado_USD'] > 0].copy()         perdedores = resumen_stats[resumen_stats['Resultado_USD'] <= 0].copy()          # --- 3. MÉTRICAS PONDERADAS (ENCABEZADO) ---         def ponderado(df):             if df.empty or df['Inversion_Inicial'].sum() == 0: return 0             return (df['Rendimiento_%'] * df['Inversion_Inicial']).sum() / df['Inversion_Inicial'].sum()          st.subheader("📌 Análisis de Gestión y Riesgo")         m1, m2, m3, m4 = st.columns(4)         m1.metric("Prom. Ponderado Ganancia", f"{ponderado(ganadores):.2f}%")         m2.metric("Prom. Ponderado Pérdida", f"{ponderado(perdedores):.2f}%")         m3.metric("Avg Trade Size (s/10k)", f"{resumen_stats['Size_vs_10k_%'].mean():.2f}%")                  p_factor = ganadores['Resultado_USD'].sum() / abs(perdedores['Resultado_USD'].sum()) if not perdedores.empty else 0         m4.metric("Profit Factor", f"{p_factor:.2f}x")          # --- 4. CUADRANTE SIMÉTRICO DE GRÁFICOS (2x2) ---         st.write("---")         fig_size = (8, 5) # Tamaño uniforme para todos                  col_f1_1, col_f1_2 = st.columns(2)         col_f2_1, col_f2_2 = st.columns(2)          with col_f1_1:             fig1, ax1 = plt.subplots(figsize=fig_size)             sns.histplot(resumen_stats['Resultado_USD'], kde=True, color='teal', ax=ax1)             ax1.set_title("Distribución de Ganancia/Pérdida (USD)")             st.pyplot(fig1)          with col_f1_2:             fig2, ax2 = plt.subplots(figsize=fig_size)             ax2.pie([len(ganadores), len(perdedores)], labels=['Wins', 'Losses'],                      autopct='%1.1f%%', colors=['#2ecc71', '#e74c3c'], startangle=140)             ax2.set_title("Efectividad (Win Rate %)")             st.pyplot(fig2)          with col_f2_1:             fig3, ax3 = plt.subplots(figsize=fig_size)             colors = ['#2ecc71' if x > 0 else '#e74c3c' for x in resumen_stats['Rendimiento_%']]             ax3.scatter(resumen_stats.index, resumen_stats['Rendimiento_%'], c=colors, alpha=0.6)             ax3.axhline(0, color='black', lw=1, ls='--')             ax3.set_title("Consistencia: ROI % Individual")             st.pyplot(fig3)          with col_f2_2:             fig4, ax4 = plt.subplots(figsize=fig_size)             equity = 10000 + resumen_stats['Resultado_USD'].cumsum()             ax4.plot(equity.values, color='royalblue', lw=2.5)             ax4.fill_between(range(len(equity)), equity, 10000, color='royalblue', alpha=0.15)             ax4.axhline(10000, color='red', lw=1, ls='--')             ax4.set_title("Curva de Equidad (Base 10k USD)")             st.pyplot(fig4)          # --- 5. MONTE CARLO CON MEDIANA (P50) ---         st.write("---")         st.write("### 🎲 Proyección Monte Carlo & Mediana (P50)")         n_sim = st.sidebar.slider("Simulaciones", 100, 1000, 500)                  fig_mc, ax_mc = plt.subplots(figsize=(16, 7))         all_paths = []         for _ in range(n_sim):             draws = np.random.choice(resumen_stats['Resultado_USD'], size=len(resumen_stats), replace=True)             path = 10000 + np.cumsum(draws)             all_paths.append(path)             ax_mc.plot(path, color='gray', alpha=0.03)                  # Mediana Estadística         median_p50 = np.median(all_paths, axis=0)         ax_mc.plot(median_p50, color='gold', lw=4, label="Mediana P50 (Camino más probable)")         ax_mc.axhline(10000, color='red', ls='--', label="Breakeven (10k)")         ax_mc.legend()         ax_mc.set_ylabel("Capital USD")         st.pyplot(fig_mc)          # --- 6. LISTADO DE TRADES DETALLADO ---         st.write("---")         st.subheader("📜 Detalle de Operaciones Cerradas")                  # Formateo de visualización (Sin afectar cálculos)         df_disp = resumen_stats.copy()         df_disp['Fecha_In'] = df_disp['Fecha_In'].dt.date         df_disp['Fecha_Out'] = df_disp['Fecha_Out'].dt.date                  fmt = {             'Inversion_Inicial': '${:,.2f}', 'Resultado_USD': '${:,.2f}',              'Rendimiento_%': '{:.2f}%', 'Size_vs_10k_%': '{:.2f}%',             'Precio_Entrada': '{:.2f}', 'Cant_Total': '{:.4f}'         }                  cols_tab = ['Ticker', 'Fecha_In', 'Fecha_Out', 'Cant_Total', 'Precio_Entrada', 'Inversion_Inicial', 'Size_vs_10k_%', 'Resultado_USD', 'Rendimiento_%']                  t1, t2 = st.tabs(["✅ Ganadores", "❌ Perdedores"])         with t1:             st.dataframe(df_disp[df_disp['Resultado_USD'] > 0].sort_values('Resultado_USD', ascending=False)[cols_tab].style.format(fmt), use_container_width=True)         with t2:             st.dataframe(df_disp[df_disp['Resultado_USD'] <= 0].sort_values('Resultado_USD', ascending=True)[cols_tab].style.format(fmt), use_container_width=True)      except Exception as e:         st.error(f"⚠️ Error en el procesamiento: {e}")         st.write("Columnas detectadas en el archivo:", list(df_trades.columns))
